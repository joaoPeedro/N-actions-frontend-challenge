import type { VehicleDetailContract } from "@shared/api/contracts/vehicle-detail.contract";

export function mapVehicleDetailContractToDomain(contract: VehicleDetailContract): VehicleDetailContract {
  return contract;
}
